# Memory

Small, categorized markdown files the AI reads before analysis and updates when
useful (Phase 2/3). Keep each file focused; do not dump everything in one file.

**Hard rule:** the AI must NOT rewrite critical risk rules (`risk/risk-rules.md`)
without explicit user approval. Risk parameters live in `.env` / config, not here.
